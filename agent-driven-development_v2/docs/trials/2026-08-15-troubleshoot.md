# 実戦トライアル: troubleshoot（2026-08-15）

サンドボックスに SDD 実行中の状態（feature ブランチ・worktree・specs/）を再現し、操縦者サブエージェントに
troubleshoot の SKILL.md をパスで渡して実走させた。題材2本。

- 題材A: worktree 割り込み — 「implement がテストを書かずに進めた。直して PR にして」
- 題材B: 却下パス — 「specify がユーザーストーリーを書いてくれなかった」（実は入力不足）

## 【効いた】

- **troubleshoot:手順6（fix ブランチを main から分岐）** — 操縦者は `feature/2026-08-14-notes` 上にいた。
  「不具合報告は feature ブランチ上で起きるのが典型」の警告がなければ、未完成機能の差分を PR に混入させていた。**実害を1件防いだ**
- **troubleshoot:手順6（gh 不在時のフォールバック）** — `gh: command not found` に遭遇しても停止せず着地できた
- **maintain:契約表「テンプレート同梱」の二重管理禁止** — 骨格節の一覧を SKILL.md に書く誤設計を止め、
  追随先に auto 審査観点が明記されていたためフルオート経路の同型欠陥にも同時に気づけた
- **troubleshoot:手順2（sdd-improver への委譲）** — 自己診断では見えなかった「検算が auto にだけ存在する非対称性」を委譲先が発見。委譲の価値が実測された
- **troubleshoot:手順1（maintain-log の事前確認）／原則（1件=1PR・state.md 不更新）** — 動作が一意で、ついで改善の切り離しコストがほぼゼロだった

## 【矛盾】【不足】【過剰】→ 反映済み

- HARD-GATE が着手明示の依頼でも無条件に確認を要求 → 例外規定を追加
- 手順0の確認に添える内容の粒度が未規定 → 規定
- 手順2の `sdd-improver` 派遣手段が未記載（`subagent_type` では解決不可）→ 明記（maintain が正）
- 手順5の検証の実行主体が未定義（maintain の自己審査禁止と衝突）→ 機械的検証/判断を伴う評価で線引き
- 原則「1件=1PR」の"1件"が未定義でスコープを自律決定できず → 「報告された事象1つ」と定義
- 手順6のリポジトリ同定にコピー判定の手段がない → 判定材料を明記。worktree 記述は圧縮

## 別件（troubleshoot 外・未対応）

サンドボックス上の既存スキルに、**テスト不在のまま完了まで通る経路**が発見された。
`analyze` の書式適合検査の欠落・`implement` の「本体検証」未定義・`review` の重大度に「テストが存在しない」が無い・
`verify` が手動 PASS でテスト不在を免責、の4段が素通りし、検算は `auto` の審査観点表にしか存在しないという非対称。
今回の事象とは因果関係がないため PR には含めず、`maintain` 送りとする。
