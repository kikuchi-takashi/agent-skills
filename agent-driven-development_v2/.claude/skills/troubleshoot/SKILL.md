---
name: troubleshoot
description: "実運用中に SDD スキル（sdd/specify/clarify/plan/tasks/analyze/implement/review/verify/auto/maintain/receive-review 等）が想定外の挙動をしたときに、証拠に基づいて原因を切り分け、修正案をユーザー承認の上で適用し、専用ブランチで PR を作成する。日本語トリガー例:「〇〇スキルがうまく動かなかった」「スキルの動きがおかしいので改善案を出してPRにして」「さっきの挙動、想定と違ったので直して」。意図的な改善依頼（「スキルを改善して」）は maintain へ。フェーズスキルではないため sdd からは呼ばれない"
---

# troubleshoot — 不具合報告からのスキル改善と PR 作成

`maintain` は「改善しよう」という能動的な依頼を扱う。このスキルは逆に、**実運用中に何かがおかしかった**
という受動的な報告を起点にする。原因切り分けとチェックリストは `maintain` に従い、
このスキル固有の責務は「証拠の確認」と「専用ブランチでの PR 作成」。

## 前提

- 開始前に `.claude/skills/maintain/SKILL.md` を読む。契約表と変更種別チェックリスト（A〜D）がここでの正
- 「スキルを改善して」のような能動的・網羅的な改善依頼は `maintain`（+ 必要なら `sdd-improver` エージェント）に譲る。
  こちらは「動かしてみたら想定外だった」という具体的な1件の事象が起点

## 手順

0. **実行確認（最初に必ず）**: 診断・修正に進む前に「troubleshoot を実行するか、スキップするか」を
   ユーザーに確認する。誤動作の疑いを検知した／報告されただけで自動的に手順1以降へ進まない。
   スキップが選ばれたら何もせず終了する（`docs/maintain-log.md` への記録も不要）
1. **事象の確認（証拠必須）**: 何をしたか・期待した挙動・実際の挙動・関連スキル名/フェーズ/該当する
   state.md やレポートの箇所を確認する。再現できる具体例なしに次へ進まない
   （`maintain` の失敗パターン「ループの空転」＝証拠なしの「たぶんこう直すと良い」を踏襲しない）
2. **原因切り分け**: 該当スキルの SKILL.md と `maintain` の契約表を突き合わせ、単一スキルの欠陥か
   スキル間境界の契約違反かを判定する
3. **修正案の作成**: `maintain` の変更種別チェックリスト（A〜D）に従う。契約表のどれかに触れるなら
   「追随が必要な場所」を同一変更に含める（1ファイルだけ直すのは最頻の欠陥源）
4. **ユーザーに提示・承認**: 診断結果・修正差分・触れるファイル一覧を提示し、適用してよいか確認する。
   無断で適用や PR 作成に進まない
5. **適用と検証**: 承認後に変更を適用し `scripts/validate` を実行し `VALIDATE OK` を確認。
   description を変えた場合は発火シミュレーション（独立したトリガー例3フレーズ以上で判定）
6. **ブランチ〜PR**: `fix/skill-<対象スキル名>-<短い説明>` のような専用ブランチを切り、
   （現在のブランチが未コミットの作業を持つ場合は先に確認してから）変更をコミットし push して
   `gh pr create` で PR を作る。push・PR 作成の前に必ずユーザーへ最終確認を挟む。
   PR 本文は「事象（証拠）→ 原因 → 修正内容 → 検証結果（validate OK）」の順で書く
7. **却下時の記録**: 診断の結果スキル側の欠陥ではないと判断した場合は修正せず、
   `docs/maintain-log.md` に日付・報告内容・却下理由を1行追記して終える

## 原則

- 実行前に必ず「やる/スキップ」の意思確認をする。誤動作の検知や報告だけで診断・修正を開始しない
- 証拠なき改善案を書かない: 再現例や実際の出力なしに「こうすべき」を提案しない
- 単一スキル修正で終わらせない: 契約表に触れる変更は境界の追随箇所も同一 PR に含める
- 1件の不具合 = 1 PR。ついでの改善を混ぜて発散させない
- push・PR 作成は不可逆に近い操作 → 適用前に必ずユーザー確認を挟む
